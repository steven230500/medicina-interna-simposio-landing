/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://simposiomedicinaunivalle.com", // Replace with actual domain if known, or keeping user placeholder
  generateRobotsTxt: true,
  generateIndexSitemap: false,
  outDir: "./out", // Assuming static export or default next build
};
